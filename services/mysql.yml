version: '2'

services:
    mysql:
        image: mysql:${DOCKER_MYSQL_VERSION}
        mem_limit: ${DOCKER_MYSQL_RAM}
        hostname: ${COMPOSE_PROJECT_NAME}_mysql_1
        networks: [lamp]
        environment:
            MYSQL_ROOT_PASSWORD: ${DOCKER_MYSQL_ROOT_PASSWORD}
        volumes:
            - ./mysql:/var/lib/mysql
            - ./conf/mysql-override:/etc/mysql/conf.d
